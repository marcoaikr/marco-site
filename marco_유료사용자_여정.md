# 마르코 유료 사용자 여정 (2026-08-06 설계 · 2026-08-16 업데이트)

> 문제의식: 유료 사용자의 이용 경로가 폼 3개+챗봇+수동 단계로 복잡해, 만든 사람도 헷갈린다. 이 문서는 **"이메일 하나로 통하는 단순한 여정"**으로 재정리하고, 실제로 고칠 것을 목록화한다.
>
> **2026-08-16 기준: 결제 신청폼의 이용자DB 자동 등록이 완성되어, "무료 진단 없이 직행 결제한 사람이 DB에 안 남던 갭"이 막혔다. 입금 확인 체크리스트도 실제 필드명으로 확정됨.** 상세는 8장 참조.

---

## 0. 두 가지 확정 원칙

**원칙 1 — 이메일 = 유일한 열쇠.**
모든 접점(진단·챗봇·요청)이 가입 이메일 하나로 돌아간다. 챗봇은 이메일 인증. 사용자가 외우거나 입력할 것은 이메일뿐.

**원칙 2 — 입금 확인은 수동, 안내는 자동.**
계좌이체 입금 확인은 당분간 운영자 수동(향후 카드결제 도입 시 자동화). 대신 "입금 대기 안내"와 "이용 시작 안내" 메일로 공백을 메운다.

---

## 1. 전체 여정 한눈에

```
[무료 체험]
무료 진단 (진단 폼, 문지기) → 이용자DB 자동 등록 → 리포트 수신
    ↓ (더 쓰고 싶다 / 무료 소진)
[결제]
결제신청 폼 제출
   → 이용자DB 자동 등록/업데이트 (상태=입금대기)   ← 2026-08-16 신규
   → "입금 대기 안내" 메일(자동) → 계좌 입금
    ↓
운영자 입금 확인(수동) → 이용자DB 플랜 전환 + 결제기록 작성(수동)
    ↓
"이용 시작 안내" 메일(수동 발송)
    ↓
[이용 — 전부 이메일 하나로]
├ 진단·재진단     → 진단 폼   (이메일로 Pro 자동 인식)
├ 상담·리포트 해석 → 챗봇     (이메일 인증)
└ 제작물 요청      → 요청 폼   (이메일로 식별, 월 10회 공유 카운터)
```

---

## 2. 등록이 일어나는 지점 (3개 진입 폼이 곧 등록)

별도 회원가입 폼 없이, 각 진입 폼이 이용자DB 등록을 수행한다. **이메일이 유일 키.**

| 진입 폼 | 등록 동작 | 방식 |
|---|---|---|
| 무료 첫 진단 폼 (f18m3zrtk8) | 이메일로 조회 → 없으면 생성, 무료사용횟수 추적 | Zite `Db` SDK |
| 결제 신청폼 (kdzglamayn) | 이메일로 조회 → 없으면 생성(상태=입금대기), 있으면 상태만 갱신 | Zite `Db` SDK |
| 요청하기 폼 (bcxxmmpdv4) | Pro 검증 + 월요청 차감 + 요청기록 저장 + 이용자 링크 | Airtable REST |

> 세 폼 모두 이메일로 이용자를 식별한다. 결제·진단·요청·챗봇에서 **같은 이메일**을 써야 한 사람으로 묶인다(다르면 갈라짐 — 결제폼 안내 문구로 유도).

---

## 3. 결제 신청폼 이용자DB 등록 로직 (2026-08-16 완성)

지난 갭: 결제폼이 Make 웹훅만 보내고 이용자DB 등록은 TODO 주석 처리 → 무료 진단 없이 직행 결제한 사람이 DB에 안 남아, 입금 확인 시 전환할 대상이 없었다.

**해결(A안):** 결제폼 endpoint에 Zite `Db` SDK로 이용자DB 조회/생성 로직을 활성화.

```
제출 → 이메일(_1)로 이용자DB 조회
  ├ 있으면: 상태(_10)만 '🟠 입금대기 (결제 신청, 입금 확인 전)'로 갱신
  │         → 플랜(_6)·다른 필드는 건드리지 않음 (입금 전엔 플랜 미확정)
  └ 없으면: 새 레코드 생성
            이름(_)·이메일(_1)·상태(_10)=입금대기·무료사용횟수(_17)=0
            → 플랜은 미설정
Make 웹훅(종류=결제신청) 전송은 그대로 유지 → 운영자 알림 메일
```

- 필드코드: `_1`=이메일, `_6`=플랜, `_10`=상태, `_17`=무료사용횟수 (이 앱의 Zite 스키마 기준)
- 이용자DB 상태 필드에 `🟠 입금대기 (결제 신청, 입금 확인 전)` 옵션 신설
- **테스트 통과**: 기존 이메일 → 중복 생성 없이 상태만 갱신 / 신규 이메일 → 새 레코드 생성(플랜 빈칸, 무료사용횟수 0) 확인

**설계 의도:** 결제폼은 "신청 접수"까지만 책임진다. 플랜 확정·활성화는 입금 확인 후 운영자가 수동 처리(컨시어지 모델).

---

## 4. 결제기록 처리 방침 (2026-08-16 확정)

- 결제기록 테이블은 **입금 확인 시점에 운영자가 수동으로 작성**한다(신청 시점 자동 생성 안 함).
- 이유: 실제 입금된 건만 기록 → 데이터가 깨끗하고, 초기엔 자동화 불필요.
- 결제폼·Make 모두 결제기록을 만들지 않는다. 이용자DB 상태(입금대기)만으로 신청을 추적.

---

## 5. 이용 단계 (전부 이메일 하나로)

- **진단·재진단** → 진단 폼. 이메일로 Pro 자동 인식, 클라이언트는 브랜드명으로 구분되어 이력 누적.
- **상담·리포트 해석** → 챗봇. 이메일 인증. 대화는 무제한(월 한도에 미포함), 일 40세션 제한만.
- **제작물 요청**(제안서·미팅 스크립트·콘텐츠 기획·미션 피드백·기타) → 요청 폼. 월 10회 카운터(진단과 공유), 요청기록에 저장 + 이용자 링크.

---

## 6. 월 한도 정의

- **월 10회 = 진단 + 요청 공유 카운터.** 챗봇 대화는 무제한(한도 미포함).
- 매월 1일 운영자가 월요청횟수를 일괄 0으로 리셋(자동화는 Airtable 유료 기능이라 수동).

### 무료 진단 하루 10건 제한 (2026-08-16 완성)

무료 진단이 하루에 몰리면 운영자 처리·API 비용이 감당 안 되므로, **무료 진단 폼에만** 하루 총량 제한을 건다(유료는 항상 접수 — 유료 우선).

```
접수 기준 = 진단기록의 진단일(_) = 오늘(KST)인 레코드 수
  ├ 신청 전 차단 (checkDailyQuota): 폼 열 때 오늘 건수 ≥ 10 → 첫 화면 마감 안내
  └ 제출 후 안전망 (submitApplication): status=NEW_FREE일 때만 재카운트 → ≥10이면 DAILY_FULL, 웹훅 미전송
유료(PRO_OK)는 이 체크를 거치지 않음 → 항상 접수
```

- 카운트 기준일: Make Create(4)에서 `진단일 = now`(Time zone Asia/Seoul) 매핑 → 한국 자정 기준으로 "오늘" 계산
- KST 날짜 계산: +9시간 후 날짜 추출
- 진단기록 primary field `_` = 진단일(Date-only)로 필터
- **주의(알아둘 점):** `진단일`은 Make가 진단기록을 *생성한* 시점 → 엄밀히는 "오늘 처리된 건수". 접수≈생성이라 대부분 일치하나, 진단 실패로 레코드가 안 생기면 카운트에서 빠질 수 있음. 지금은 이대로 두고 실운영에서 관찰(완벽히 하려면 접수 전용 카운트 테이블 필요 — 현 단계엔 과함).
- 상태: 코드·매핑·결제(Zite Pro + API) 완료. **게시(Publish) 후 폼 정상 통과 테스트로 최종 확인 예정.**

---

## 7. 진행 상황 (2026-08-18 기준)

**완료:**
- ✅ 요청 폼 완성 (Pro 검증·월요청 차감·요청기록 저장·이용자 링크·운영자 알림)
- ✅ 챗봇 이메일 인증 전환
- ✅ 이용 시작 안내 메일 / 입금 대기 안내 강화
- ✅ 요청 폼 정리(재진단 제거·3갈래 안내) + 운영자 알림
- ✅ **결제 신청폼 이용자DB 자동 등록 (직행 결제자 갭 해소)**
- ✅ **입금 확인 운영자 체크리스트 확정(실제 필드명)**
- ✅ **결제기록 = 입금 확인 시 수동 작성 방침 확정**
- ✅ **무료 진단 하루 10건 제한 로직 (신청 전 차단 + 제출 후 안전망, 유료 우선)** — 코드·매핑·결제 완료, 게시 후 폼 테스트만 남음
- ✅ **Claude API 장애 대비 에러 핸들러** — 진단 시나리오 HTTP(3, Claude 호출)에 에러 갈래 추가. 실패 시 사용자에게 지연 안내 메일(Gmail 34) + 운영자에게 실패 알림(Gmail 35). "장애 시 사용자 감감무소식 방치" 구멍 해소. **테스트만 남음(URL 오타로 에러 갈래 작동 확인).**
- ✅ **마르코 CRM 대시보드 고도화 (Zite)** ← 이번 세션. 집계 버그 수정 + 운영 카드 + 마르코 인사이트 + 이용자 상세 드릴다운. 상세는 10장.
- ✅ **챗봇 이메일 인증 실제 작동 검증** ← 이번 세션. 강감찬을 임시 Pro로 바꿔 테스트 → 이메일 인증 통과 + AI 답변 정상 생성 확인. 지난 API 키 403 문제도 현재 해소됨. (테스트 후 강감찬 원상복구 필요)
- ✅ **운영자 대시보드 보안 (Internal app 전환)** ← 이번 세션. Zite Access를 Public → Internal로 변경. 조직 멤버(=흑상어쌤)만 Zite SSO 자동 인증으로 접근, URL만 아는 외부인 차단. 별도 로그인 코드 불필요. **Publish 필요 + 시크릿창으로 잠금 검증 필요.**
- ✅ **랜딩 건당 카드에 '진단 없이 건당 바로 신청' 버튼 추가** ← 이번 세션. Pro 카드와 동일 디자인·링크(ut4ec57xwv.zite.so). 이제 3개 카드(건당·Pro월·Pro연) 모두 "무료 진단으로 시작 + 진단 없이 바로 신청" 두 경로 일관 제공.

**미완 / 나중:**
- ⬜ **Pro 사용자 본인용 대시보드** — 운영자용 드릴다운 완성됨. 본인용은 인증(이메일+확인코드)·데이터 격리·보안 설계가 필요한 별도 작업. 첫 Pro 사용자 생기면 착수. (챗봇 이메일 인증 로직이 참고 기반)
- ⬜ 대시보드 Publish (게시) — 현재 Preview 상태. 라이브 반영 필요 (Internal app 보안 전환도 Publish해야 적용됨)
- ⬜ 대시보드 시간축 추이(일/주/월), 업종×병목 히트맵, CSV 내보내기 — 데이터 쌓이면
- ⬜ **Claude 장애 대비 에러 핸들러 테스트** — HTTP(3) URL에 오타 넣어 에러 갈래 작동(메일 2통) 확인 후 원복
- ⬜ 결제기록 구독만료일 자동 계산(Formula) — 원하면 추가
- ⬜ 매월 리셋 자동화(사용자 늘면 Airtable 유료 Run script 또는 Make)
- ⬜ 입금 확인 반자동/카드결제 전환(향후)

**메모:**
- 플랜 옵션 표기 띄어쓰기(`🟣 Pro 월정액` 등) — 코드의 Pro 판정이 `includes('Pro')`로 작동 중. 근본 통일은 여러 곳 영향이라 지금은 건드리지 말 것.
- 대시보드 집계도 같은 이유로 `includes` 부분 일치 사용(정확 일치 `===`는 이모지·괄호 때문에 항상 0 나옴 — 이번에 수정).

---

## 8. 운영자 입금 확인 체크리스트 (요약 — 상세는 별도 문서)

```
[매월 1일]
□ 이용자DB 월요청횟수 전체 0으로 리셋

[입금 확인 시마다]
0. 입금 대조 — 알림 메일 이름·플랜·시간 ↔ 계좌 입금자명·금액
1. 이용자DB 수정 — 상태 입금대기 → 플랜에 맞는 상태 / 플랜 입력 / 월요청한도=10(Pro) / 월요청횟수=0
2. 결제기록 추가 — 이용자DB 링크·플랜·결제금액·결제상태=입금완료·입금자명·결제구분·결제일·구독시작/만료일
3. 이용 시작 안내 메일 발송
```

> 실제 필드명 기준 상세 버전: `마르코_입금확인_체크리스트.md`

---

## 9. Claude API 장애 대비 (2026-08-17 추가)

**문제의식:** 마르코 진단 엔진은 Claude API에 전적으로 의존한다. Claude가 죽으면(실제 8/17 오전 장애 발생) 마르코도 죽고, 사용자 눈엔 "클로드 장애"가 아니라 "마르코가 안 됨"으로 보여 신뢰 타격은 온전히 마르코가 받는다.

**기존 구멍:** 진단 시나리오 HTTP(3, Claude 호출)에 에러 핸들러가 없어, Claude 실패 시 시나리오가 그 지점에서 멈춤 → Router 이후(진단기록·리포트·차감) 전부 실행 안 됨 → **사용자는 아무 메일도 못 받고 방치**(감감무소식). 반면 채널 실측 HTTP들(YouTube·홈페이지 등)엔 Resume 핸들러가 있으나, 그건 보조 데이터라 무시해도 되는 경우.

**해결(구현 완료):** HTTP(3)에 에러 갈래 추가.
```
HTTP(3) Claude 호출
  ├─(정상)→ Router(12) → 진단기록·리포트·차감 …   ← 기존 그대로
  └─(에러)→ Gmail(34) 사용자 지연 안내
           → Gmail(35) 운영자 실패 알림           ← 신규
```
- 사용자 메일: "신청 정상 접수됐고, 일시적 부하로 지연 중. 복구되는 대로 보내드림." → **"실패"가 아니라 "곧 보낸다"로 프레이밍.**
- 운영자 메일: 신청자 이메일·시각 + "복구 후 수동 재실행" 지시.
- 컨시어지 모델이라 "실패해도 사람이 챙긴다"가 오히려 신뢰가 됨.

**주의:** HTTP(3) 본체는 새로 만들거나 지우면 하위 수식 `3.Data.content[1].text` 참조가 끊김(고스트 필) → 우클릭으로 핸들러만 추가함.

**보완 아이디어(미적용):**
- 완료 화면·메일에 도착 예상 시간 상시 명시 → 기대치 관리(지연돼도 당황 안 함)
- 큰 장애 인지 시 폼/랜딩 상단에 수동 배너 → 신청 전 안내
- 마르코가 Claude 장애를 실시간 자동 감지하는 장치는 현재 없음(수동 인지 의존)

---

## 10. 마르코 CRM 대시보드 (2026-08-18 추가)

**플랫폼:** Zite (마르코 대시보드 앱). Airtable 이용자DB(Db)·클라이언트DB(Db1)·결제기록(_2)·요청기록 연결. 운영자(흑상어쌤) 전용 관리자 화면.

**목적:** "숫자 세기"를 넘어 ① 운영 효율(지금 뭐 할지) ② 마르코 고유 인사이트(업종·고민 = 콘텐츠·뉴스레터 소재) ③ 개별 사용자 케어를 한 화면에서.

### 이번 세션 완료 (4단계)

**1) 집계 버그 수정**
- 문제: 상태·플랜을 정확 일치(`=== '🔵 활성'`)로 매칭 → 실제 옵션은 이모지+괄호 전체 문자열이라 항상 0.
- 해결: `includes()` 부분 일치로 전환. 활성 이용자 = 상태에 '활성' 또는 '건당이용' 포함(돈 낸 사람 전부). 플랜/상태 분포도 includes로 수정 + '입금대기' 카운트 추가.
- 결과: 비어 있던 차트가 채워짐.

**2) 운영 카드 3개 추가 (1층)**
- 입금대기 건수(⚠️ 주황) — 운영자가 지금 처리할 일
- 무료체험 이용자 — 전환 잠재 대상
- 유료 전환율 — (돈 낸 이용자 ÷ 전체)×100

**3) 마르코 인사이트 섹션 (금광)**
- 업종별 클라이언트 분포 (클라이언트 업종 = r._1) — 현재 뷰티/헬스 압도적, 교육·요식 다음
- 클라이언트 고민 5축 분석 (긴급문제 = r._18) — urgentProblem 텍스트를 마르코 5축 키워드로 분류(유입/타깃/신뢰/메시지/재구매+기타). 현재 유입·기타 위주(표본 적음).
- → 이 데이터가 뉴스레터·콘텐츠·콜드메일 소재. 예: "이번 달 마케터 최대 병목: 유입"

**4) 이용자 상세 드릴다운 (운영자용)**
- 이용자 관리 목록에서 클릭 → `/users/:userId` 상세 화면
- 기본정보 / 이번 달 사용량(월요청횟수·잔여·클라이언트수) / 활동요약(대기·처리중·완료) / 요청 이력 타임라인(요청기록에서 이메일 필터) / 요청 종류 분포
- 요청기록 테이블 연결해서 개별 사용 이력 표시

**5) 보안 — Internal app 전환**
- Zite Access를 Public(누구나 링크로 접근) → **Internal(조직 멤버만)** 로 변경.
- 조직 멤버(=흑상어쌤)만 Zite SSO 자동 인증으로 접근. URL만 아는 외부인은 차단.
- Zite가 인증을 처리하므로 별도 로그인 코드 불필요. Sign-in: Magic link·Google 기본 활성(SSO는 Enterprise 전용).
- ⚠️ Publish 후 시크릿창으로 "외부 차단 / 나는 접근" 검증 필요.

### 대시보드 endpoint (Zite)

- Get_Users (이용자DB, Db): 필드코드 _=이름, _1=이메일, _3=회사, _6=플랜, _10=상태, _13=클라이언트수, _19=무료사용횟수
- Get_Clients (클라이언트DB, Db1): _1=업종, _18=긴급문제(고민), _12=채널, _14=월매출, _15=광고예산
- Get_Payments (결제기록, _2)
- Get_Dashboard_Stats: 위를 집계

### 다음 작업 (대시보드)

- ⬜ **Publish** — 현재 Preview 상태, 라이브 반영 필요 (Internal app 보안도 Publish해야 적용)
- ⬜ 시간축 추이(일/주/월 신규·진단), 전환 퍼널 — 데이터 쌓이면
- ⬜ 업종×병목 히트맵 (urgentProblem을 Claude API로 5축 분류하면 자동 생성) — 고도화
- ⬜ CSV 내보내기 (뉴스레터·분석용)
- ⬜ 입금대기 원클릭 처리 (대시보드에서 상태 변경)

### Pro 사용자 본인용 대시보드 (별도 프로젝트 — 미착수)

- 운영자용 드릴다운은 완성. 본인용은 **인증·보안이 핵심**이라 별도 설계 필요.
- **인증 방침 확정:** 챗봇 = 이메일만(마찰 최소, 대화라 저위험). 대시보드 = **이메일 + 확인 코드**(개인 사업 데이터라 고위험 → 한 겹 강화).
- 필요 작업: ①이메일+코드 로그인 ②데이터 격리(서버에서 본인 이메일만 필터, 절대 남의 데이터 안 섞이게) ③Pro 권한 확인 ④본인 관점 화면(내 사용량·내 클라이언트·내 요청·내 리포트) ⑤진입 URL.
- 난이도 높음(인증·보안 사고 리스크). **첫 Pro 사용자 생기면 실수요 반영해 착수** 권장.
- 참고: 챗봇 이메일 인증(이용자DB 조회 → Pro 확인) 로직이 이미 검증됨. 단 챗봇=Cloudflare Worker, 대시보드=Zite로 인프라가 달라 코드 복사는 안 되고 방식만 참고.
