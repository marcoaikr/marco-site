# 마르코(MARCO) 서비스 아키텍처 · 프로세스 설계

> **문서 목적**: 여러 세션에 걸쳐 확정된 마르코의 데이터 흐름·프로세스·Make 재설계 방향을 한 곳에 고정한다. 세션이 바뀌어도 이 문서를 기준으로 이어간다.
> **최종 확정일**: 2026-07-28
> **상태**: 아키텍처 확정 완료 → 다음 단계는 Zite Submit 코드에 문지기 로직 삽입

---

## 0. 한 줄 요약

폼(Zite) → **하나의 Airtable(마르코_MVP)** → Make(진단 생성 + 검수 + 발송).
"진단 생성"과 "사용자 발송"을 분리하고, 그 사이에 **사람 검수**를 둔다. 무료/유료의 차이는 **결제 확인 단계의 유무**뿐이다.

---

## 1. 확정된 데이터 아키텍처 (스크린샷으로 검증 완료)

```
   Zite 폼 A / 폼 B
        │  (Submit Application 코드가 직접 기록)
        ▼
┌─────────────────────────────┐
│  Airtable  "마르코_MVP"  (단일 진실)  │
│  - 이용자DB / 클라이언트DB / 진단기록      │
│  - 미션기록 / 결제기록 / 미션라이브러리 / 벤치마크DB │
└─────────────────────────────┘
        ▲
        │  (Make도 같은 Airtable을 읽고 쓴다)
   Make 시나리오 (A/B/C)
```

**핵심 확정 사실:**
- Zite Database 탭 = "This app is pulling data from Airtable" → Zite의 `Db`/`Db1`은 **내장 DB가 아니라 외부 Airtable 커넥터**다.
- Make Airtable 모듈 = Base `마르코_MVP`, Table `클라이언트DB` 등 → **Zite와 동일한 Airtable을 본다.**
- **이중 DB 아님. 데이터 엇갈림 없음.** 하나의 Airtable을 Zite와 Make가 공유한다.

---

## 2. 현재 상태 진단 (재설계가 필요한 이유)

| 문제 | 증거 | 조치 |
|---|---|---|
| **진단 엔진이 꺼져 있음** | Make 시나리오 "Integration Webhooks" 토글 OFF(회색) | 재설계 후 재가동 |
| **문지기 로직 부재** | Submit 코드가 이메일 조회 없이 제출마다 무조건 새 레코드 생성 | Zite Submit 코드에 문지기 삽입 |
| **테스트 쓰레기 누적** | 이용자DB 74건 중 "홍길동" 6개 + `dsa`·`ㅁㄹㅇ` 등 | 실 운영 데이터만 남기고 삭제 |
| **웹훅 이중 발송** | Submit 코드가 `dfry…` + `zdho…` 두 웹훅에 POST | `dfry…` 라인 코드에서 제거 |
| **검수 지점 없음** | 기존 시나리오가 Claude → 사용자에게 바로 Gmail | "마르코 검수 → 수동 발송" 구조로 분리 |
| **결제 게이트 없음** | 유료도 제출 즉시 진단 | 입금 확인 후 트리거로 변경 |

---

## 3. 포지셔닝 (방향)

- **메인 메시지**: "기억하는 세 번째 뇌" → **"판단 기준을 주는 AI 사수"** 로 무게중심 이동.
- 이유: "AI가 클라이언트를 기억한다"는 차별점은 Claude Projects 등으로 이미 침식됨. 범용 AI가 못 주는 것은 기억이 아니라 **"이게 잘한 건지 일관되게 판정하는 시니어의 눈"**.
- 마르코의 진짜 해자 = 18년 실무 + 저서가 인코딩된 **5차원(FC/TG/TR/MS/RT) + 9블록 + 병목 로직 + 미션 루프**라는 **반복 가능한 판단 체계/워크플로우**.

---

## 4. 5가지 프로세스 원칙 (운영자 정의)

**(1) 무료 첫 진단** — 신규만, 1회. 식별(이메일)·기록 저장 필수.
**(2) 건당(재이용)** — 무료 1회 사용 확인 → 유료 안내 → 건당 선택 → 입금 안내 → 입금 확인 → 진단.
**(3) 구독(월/연)** — 무료 1회 확인 → 유료 안내 → 구독 선택 → 입금 안내 → 입금 확인 → 진단. **진단서뿐 아니라 제안서 초안·미팅 자료 등 요청유형 선택 가능**, 월 이용횟수 관리.
**(4) 반자동 + 사람 검수** — 무료·유료 모두 결과물을 **마르코에게** 먼저 보내고, 검수·수정 후 사용자에게 발송. (수정이 사라지는 시점에 완전 자동화)
**(5) 유료 수동 처리** — 입금 확인 후 수동 요청 → 결과 확인 → 수동 발송.

**관통 원칙**: "진단 생성"과 "사용자 발송" 분리 + 그 사이 사람 검수. 무료/유료 차이는 결제 확인 유무뿐.

---

## 5. 3가지 사용자 여정

| | 식별·기록 | 결제 | 진단 생성 | 사용자 발송 | 운영자 개입 |
|---|---|---|---|---|---|
| **A. 무료 첫 진단** | 신규 등록 | 없음 | 자동(마르코 뇌→마르코 메일) | 검수 후 수동 | 검수 1회 |
| **B. 건당** | 기존 확인 | 입금 확인 | 입금 후 자동 | 검수 후 수동 | 입금확인+검수 |
| **C. 구독(월/연)** | 기존 확인 | 입금 확인(첫 회) | 입금 후 자동, **요청유형 분기** | 검수 후 수동 | 입금확인+검수 |

---

## 6. Make 재설계 — 3개 시나리오로 분리

기존 "한 줄짜리 시나리오"를 역할별 3개로 쪼갠다. **접수 ≠ 진단 생성 ≠ 발송.**

### 시나리오 A — 접수 라우터 (문지기의 Make 측)
> 진단 안 돌림. 판단·기록·알림만. (문지기 핵심은 Zite Submit 코드가 담당 — 8장 참조)
```
Webhook(제출)
 → Airtable: 이메일로 이용자 조회
 → Router:
    · 신규            → 이용자·클라이언트 생성 → 마르코 "무료접수" 알림 → 시나리오 B 트리거
    · 기존+무료소진    → 클라이언트(🟡대기) → 사용자 "유료 안내+계좌" 메일 → 마르코 "건당접수(입금대기)" 알림
    · Pro 활성+잔여>0  → 클라이언트 생성 → 마르코 "Pro접수" 알림 → 시나리오 B 트리거
    · Pro 잔여=0       → 사용자 "한도 소진" 메일
```

### 시나리오 B — 진단 생성 엔진 (마르코 뇌) + 검수 지점
> 기존 시나리오의 JSON→HTTP(Claude)→파이프 파싱을 **재사용**. 끝만 바꾼다.
```
트리거(A 호출 또는 운영자 수동/입금확인 후)
 → [요청유형 Router] 진단 / 제안서 / 미팅자료 … 별 시스템 프롬프트 분기 (구독용)
 → JSON(Claude 요청) → HTTP(Claude 호출) → 파이프 파싱
 → Airtable: 진단기록 저장 (상태 = 검수대기)
 → Gmail: 결과물을 [마르코]에게 발송   ← 사용자 아님! (검수 지점)
```

### 시나리오 C — 발송 (검수 후 수동 트리거)
```
트리거(운영자가 진단기록 상태를 "발송" 으로 변경 → Airtable Watch)
 → Gmail: 최종 리포트를 [사용자]에게 발송
 → Airtable: 진단기록 상태=발송완료, 무료사용횟수/잔여요청횟수 갱신
```

**공통**: 무료·건당·구독이 B·C를 공유. 갈리는 건 A의 분기 + "언제 B를 트리거하느냐"(무료·Pro=즉시 / 건당·신규구독=입금확인 후 수동).

**유료 입금확인 트리거 방법**: 운영자가 Airtable 결제기록의 `결제상태`를 "입금완료"로 변경 → Make가 Watch Records로 감지 → B 발동. (버튼·폼 불필요, 셀 하나 변경)

---

## 7. 상태 필드 축 (운영 단순화 핵심)

진단기록 테이블에 **상태 필드 하나**로 전 과정 관리:
`접수 → 검수대기 → (수정) → 발송 → 발송완료`
→ 마르코는 이 한 화면에서 "지금 뭘 해야 하는지"를 본다. 시나리오 A·B·C가 이 필드를 축으로 물린다.

---

## 8. 문지기 로직 — Zite Submit 코드에 둔다 (결정)

**이유**: 제출 순간 이메일을 조회해 신규/기존을 가려야 "홍길동 6개" 같은 무조건 생성 문제를 원천 차단. Zite도 같은 Airtable을 보므로 여기서 판단 가능.

**넣을 로직(개념)**:
```
제출 수신
 → Airtable(이용자DB)에서 email 조회
 → 있으면:
     · 무료소진 & 비Pro → 신규 이용자 생성 안 함, "유료 안내" 경로
     · Pro 활성 & 잔여>0 → 진단 경로, 잔여 -1
     · Pro 잔여=0 → "한도 소진" 경로
 → 없으면(신규):
     · 이용자DB 생성 + 클라이언트DB 생성 + 무료사용횟수=1 → 진단 경로
 → Make 웹훅은 zdho… 하나만 (dfry… 제거)
```

---

## 9. 재사용 자산 (버리지 말 것)

- **Claude 호출부**: JSON(Create JSON) → HTTP(`api.anthropic.com/v1/messages`) 구조.
- **모델**: `claude-sonnet-4-6`, max_tokens 4000, HTTP 타임아웃 300s.
- **시스템 프롬프트**: "당신은 MARCO… FC/TG/TR/MS/RT 5프레임 병목 진단…"
- **출력 파싱**: 파이프 구분자(`STATUS=ok|FC=…|…===REPORT===`) + Make split/trim.
- **Router STATUS 분기**: 정상 / "정보 부족" fallback.

---

## 10. 정리(Cleanup) 체크리스트

- [ ] 이용자DB 테스트 데이터 삭제 (홍길동 6개, `dsa`/`ㅁㄹㅇ`/`ㅇㄴㅇ` 등) — 실 운영 데이터만 남김
- [ ] 진단기록 테스트 데이터 13건 삭제
- [ ] Zite Submit 코드에서 `dfry…` 웹훅 POST 라인 제거
- [ ] 이용자DB `무료잔여횟수` 공식: `3 - 무료사용횟수` → `1 - 무료사용횟수`
- [ ] 폼 A(무료 첫 진단)의 Zite 연동이 폼 B(jztyy3xlul)와 공유되는 부분 정리 (진단 엔진은 공유 OK, 문지기 분기만 분리)
- [ ] 구 미션피드백폼(3btgoy4gyb) OFF, 등록폼(ut4ec57xwv) 폐기
- [ ] 랜딩 CTA: 일반 "시작" → 폼 A로 통일 / "이미 이용" → 폼 B로

---

## 11. 폼 구조 (확정)

| 폼 | 역할 | ID/비고 |
|---|---|---|
| **폼 A — 무료 첫 진단** | 신규용. 기본정보(6필드) + 진단질문 한 흐름 | jztyy3xlul 복제본. 기본정보 페이지 앞단 추가 완료 |
| **폼 B — 재이용 진단** | 기존 이용자용. 이메일 + 진단질문 | 현 jztyy3xlul 그대로 |
| **마르코에게 요청하기** | 구독 후속 요청(6유형) | bcxxmmpdv4. 제안서초안/미팅스크립트/콘텐츠기획/미션피드백/재진단/기타 |

폼 A 기본정보 6필드: 이메일(필수)·이름(필수)·전화번호(필수)·회사명(선택)·직책(선택)·개인정보동의(필수).

---

## 12. 핵심 상수

- **Airtable Base**: `마르코_MVP` (테이블: 이용자DB/클라이언트DB/진단기록/미션기록/결제기록/미션라이브러리/벤치마크DB)
- **Make 웹훅(현재)**: `https://hook.us2.make.com/zdhosvezvtp7nzde7ul8oou74u5yq4bd`
- **Make 웹훅(구/제거대상)**: `https://hook.us2.make.com/dfry21qpyed693c3s4guaizsacxlk0ba`
- **Zite 앱 편집기**: build.fillout.com/editor/zite/… (폼 A: f18m3zrtk8)
- **입금 계좌**: 신한은행 110-343-583757 (예금주 이인복)
- **이메일**: 표시/문의 hello@marcoai.kr / 실제 발송 marco.ai.kr@gmail.com (From Name "마르코 MARCO")
- **가격**: 무료 1회 / 건당 ₩9,900 / Pro 월 ₩39,000 / Pro 연 ₩396,000 / Pro 월 요청한도 10 (2026-07-28 확정, 이전 15에서 변경)
- **도메인**: marcoai.kr (Cloudflare)

---

## 13. 다음 단계

1. **Cleanup 체크리스트 중 안전한 것부터** (테스트 데이터 삭제, 무료 1회 공식)
2. **Zite Submit 코드에 문지기 로직 삽입** (8장) — 서비스의 심장, 홍길동 문제 차단
3. **Make 재설계**: 기존 시나리오 백업 → A(접수) → B(진단+검수, 기존 Claude부 이식) → C(발송) 순
4. 통합 테스트: 무료 신규 / 무료 소진 후 재이용 / Pro 각 경로

---

## 14. 해자(Moat) 전략 — 카피 불가능한 것을 만든다

> **전제**: 기능적 차별화는 AI 평준화로 오래 못 지킨다. "무엇을 해주느냐"가 아니라 **"무엇이 축적되느냐"**로 지켜야 한다.

### 3중 해자

**해자 1 — 역량 성장 = 전환비용 (제품 안에 심는다, 지금부터)**
별도 서비스가 아니라 **진단 제품 자체**에 있어야 한다. 매 진단이 답만 주는 게 아니라 "왜 이 점수인지·왜 이게 병목인지"를 **사수처럼 설명**하면, 사용자는 쓸수록 스스로 판단하는 힘이 자란다. "이 도구가 나를 성장시킨다"는 경험이 가장 강한 락인. → "AI 사수" 포지셔닝과 동일. 투자 0, 리포트 서술 방식만 바꾸면 됨.

**해자 2 — 축적 데이터 = 독점 자산 (마르코만 가진 것)**
진단이 쌓일수록 수백 개 브랜드를 FC/TG/TR/MS/RT로 채점한 데이터가 생긴다. → "당신 클라이언트의 TR은 같은 업종 평균 대비 하위 20%" 같은 **벤치마크** 제공 가능. 경쟁자는 진단을 직접 쌓아야만 생기므로 카피 불가. (이미 Airtable에 벤치마크DB 테이블 존재 = 씨앗.)

**해자 3 — 커뮤니티/네트워크 = 네트워크 효과 (가장 어렵고 가장 나중)**
방향은 맞지만 리스크 둘: ① 초기 회원 부족 → 죽은 게시판(신뢰 하락) ② AE·프리랜서는 서로 경쟁자 → 노하우 공유 꺼림. 그냥 열면 아무도 말 안 함.

### 단계적 실행 (동시 진행 금지 — 1인 리소스에서 다 하면 전부 부실)

1. **1단계 (지금)**: 진단 안에 "가르치는 사수" 심기. 투자 0. → 해자 1 씨앗.
2. **2단계 (Pro 출시 직후)**: **뉴스레터**. 커뮤니티보다 가벼움(1:다, 죽은 게시판 리스크 없음). Pro 대상 마케팅·브랜딩 정보/통계/성공사례/책 큐레이션. "유료 전용 가치"의 첫 현실적 형태.
3. **3단계 (진단 데이터 축적 후)**: **벤치마크 공개**. 해자 2 발동. "이번 달 △△업종 평균 병목은 TR" 등 → 남이 못 만드는 콘텐츠.
4. **4단계 (Pro 임계 회원 후)**: **커뮤니티/네트워크**. 경쟁자 딜레마 회피 설계(익명 벤치마크 비교 / 비경쟁 매칭).

### 랜딩·팀 페이지 반영 원칙
- **랜딩(무료 유입용)**: 장기 비전을 앞세우면 "지금 진단" 동기가 흐려짐 → **가볍게 한 조각만**. 최종 CTA 근처 "쓸수록 판단력이 자란다"(해자 1). 뉴스레터·커뮤니티는 "Pro가 되면 열리는 것"으로 가격 섹션에 짧게.
- **팀 페이지(비전의 그릇)**: 철학의 진짜 자리. "단기적으로 문제를 풀지만, 장기적으로 마케터를 성장시키고, 결국 마케터들이 서로 연결되는 곳을 지향한다"는 서사. 지금 커뮤니티가 없어도 "지향점"으로 말하는 건 정직하고 매력적.

### 한 줄 요약
마르코의 해자는 "더 좋은 도구"가 아니라 **사용자의 성장 + 마르코만의 데이터 + 시간이 지나야 생기는 네트워크**다. 카피할 수 없는 건 기능이 아니라 이 셋이다.

---

## 부록 A. 5차원 스코어링 (확정 정의)

순서 자체가 "어디서부터 막혔는지" 설명 논리: 사람이 오는가 → 맞는 사람인가 → 믿는가 → 왜 여기인가 → 다시 오는가.

| 코드 | 명칭 | 정의 |
|---|---|---|
| **FC** | 유입 채널 (Flow Channel) | 타깃 고객이 자연스럽게 오는 경로가 있는가 |
| **TG** | 타깃 명확성 (Target Clarity) | 올바른 사람에게 닿고 있는가 |
| **TR** | 신뢰 신호 (Trust Signal) | 믿고 살 수 있다는 근거가 있는가 |
| **MS** | 메시지 명확성 (Message Clarity) | 왜 이 브랜드여야 하는가가 보이는가 |
| **RT** | 재구매 구조 (Retention/Repeat) | 한 번 산 사람이 다시 오는 구조가 있는가 |

각 20점 만점, 합산 100점. 병목 = 최저점 영역(동점 시 FC→TG→TR→MS→RT 우선).

---

## 부록 B. 확정 랜딩 카피 (8섹션)

**히어로**: "이 방향이 맞는지, 물어볼 사람이 없다면 / 혼자 고민하지 마세요. 마케터의 세 번째 뇌, 마르코가 사수처럼 클라이언트를 같은 기준으로 진단하고, 더 나은 판단을 돕습니다." CTA: 무료로 첫 진단 받기 · 약 5분 · 첫 진단 무료
- 포지셔닝: "기억하는 뇌" → "판단을 돕는 AI 사수" (세 번째 뇌 = 시니어 판단 계층)
- 8섹션: 히어로 → 문제·공감 → 차별점(5차원) → 작동 3단계 → 샘플 → 가격 → FAQ → 최종 CTA
- FAQ 발송 문구: "정확도를 위해 마르코 팀이 한 번 더 확인합니다."
- 가격 순서: 무료 → 건당 → Pro월(월 10회) → Pro연
- CTA 통일: 일반 "무료로 첫 진단 받기"(폼 A) / "이미 이용? 다시 진단하기"(폼 B) / "샘플 리포트 보기"

---

## 15. 문지기 · 3개의 문 · 결제 흐름 · 운영자 절차 (2026-07-28 확정)

### 15.1 3개의 문 (사용자 유형별 진입점)

| 사용자 | 문 | URL | 처리 |
|---|---|---|---|
| 신규 / 재이용 진단 | **폼 A** (문지기 탑재) | `f18m3zrtk8.zite.so` | 문지기가 신규/무료소진/Pro 자동 분기 |
| 진단 없이 바로 결제 | **결제 신청 폼** | `ut4ec57xwv.zite.so` | 이메일+이름+플랜+동의 4항목 → 입금 안내 |
| 무료 후 결제 결심 | 리포트 이메일 링크 | (Make 발송) | 이메일 하단 결제 링크 |

- **폼 B(jztyy3xlul)는 문지기 없음 → 랜딩에서 링크 제거함.** 모든 진단은 폼 A 하나로.
- 랜딩 모든 시작 버튼 → 폼 A. Pro/연간 카드에 "바로 신청 →" 보조 링크 → 결제 신청 폼.

### 15.2 문지기 로직 (폼 A / Submit Application 코드 — 적용 완료, 방향B 반영 2026-07-30)

제출 → `Db.findOne({ filters: { _1: email } })` 로 이메일 조회 →
- **신규(없음)**: 이용자 생성(`_38`=**0**, `_6`='🟢 무료') + 클라이언트 생성 → `zdho` 웹훅 → status `NEW_FREE`
- **Pro + 잔여(`_45`)>0**: `_43`(월요청횟수) +1 → `zdho` 웹훅 → status `PRO_OK`
- **Pro + 잔여≤0**: 웹훅 안 감 → status `QUOTA_EXCEEDED`
- **기존 비Pro + 무료 미사용(`_38`<1)**: 무료 진단 허용 → `zdho` 웹훅 → status `NEW_FREE`
- **기존 비Pro + 무료 사용함(`_38`≥1)**: 웹훅 안 감 → status `NEED_PAYMENT`

**★ 차감 구조 (방향 B — "성공했을 때만 1회 차감"):**
- 문지기는 제출 시 `_38`을 **세지 않는다**(신규는 0으로 생성). 무료 차감은 **진단 성공(ok) 시 Make 성공 경로(Airtable Get(10)→Update(11), `_38+1`)에서만** 일어난다.
- 정보부족(insufficient) → Make fallback엔 +1 없음 → `_38=0` 유지 → **차감 안 됨(복구 로직 불필요).**
- 재제출: `_38=0`이면 무료 재허용, `_38≥1`이면 결제 안내. → 자연스럽게 맞물림.

핵심: 웹훅은 진단 진행(NEW_FREE/PRO_OK)일 때만 발송. `dfry` 웹훅 제거됨. `Number()`로 숫자 파싱. status를 웹훅 payload `진단유형`에 포함.

### 15.3 이용자DB 필드 키 (확정)

| 필드 | 키 | 비고 |
|---|---|---|
| 이름 | `_` | |
| 이메일 | `_1` | 식별 키 (findOne 조회) |
| 전화 | `_2` | |
| 회사 | `_3` | |
| 직책 | `_4` | |
| 플랜 | `_6` | 이모지 포함: 🟢 무료 / 🔵 건당 / 🟣 Pro |
| 무료사용횟수 | `_38` | 신규 시 0, 진단 성공 시 Make가 +1 (방향B) |
| 월요청횟수 | `_43` | 쓰기 (Pro 사용 시 +1) |
| 월요청한도 | `_44` | 입금 확인 후 운영자가 10 설정 |
| 잔여요청횟수 | `_45` | **읽기 전용** = `_44 − _43` 자동계산 |

SDK: `Db.findOne({filters:{_1:email}})` / `Db.create({record})` / `Db.update({id,record})`. findOne은 없으면 undefined.

### 15.4 결제 신청 폼 (ut4ec57xwv — 개조 완료)

- 4항목: 이메일 / 이름(입금자명) / 플랜(건당 ₩9,900·Pro월 ₩39,000·Pro연 ₩396,000) / 개인정보동의. **무료 옵션 없음.**
- 완료화면: 신한 110-343-583757(이인복) + 선택 플랜 금액 + "입금자명=이름 동일".
- 제출 시: 이메일로 findOne → 있으면 플랜 '입금대기'로 갱신, 없으면 새 레코드. **무료사용횟수·한도는 안 건드림.**
- Make 웹훅: `marco-payment` **생성·연결 완료** (진단 웹훅과 별개 시나리오 "결제신청 접수"). 웹훅 수신 → Gmail 알림 메일(운영자 marco.ai.kr@gmail.com) 발송. 시나리오 ON.
- 알림 메일: 제목 `💳 결제 신청 - {신청플랜} ({이름})`, 본문에 이름·이메일·플랜·신청시간 + 운영자 처리 안내. Body type = Raw HTML(`<br>` 줄바꿈). 신청시간은 `formatDate(신청시간; YYYY년 M월 D일 HH:mm; Asia/Seoul)`로 한국시간 표시. **실발송 확인 완료.**

### 15.5 운영자 입금 처리 절차 (표준)

입금 신청 알림 수신 → 
1. 신한 계좌 입금 확인 + 입금자명 ↔ 신청 이름 대조
2. 이용자DB에서 이메일(`_1`) 레코드 찾기
3. 입금 확인 시 설정:
   - Pro월/연: `_6`→🟣 Pro / `_44`→10 / `_43`→0
   - 건당: `_6`→🔵 건당 (한도 설정 없음)
4. 결제기록: 결제상태→입금완료, 입금자명·결제구분 기록
5. 이용 안내 메일 발송 (+ 폼 A 링크)

**불변 규칙:** ① 입금 확인 전 Pro 전환 금지 ② `_44`는 입금 확인 후에만 설정(문지기는 안 건드림) ③ Pro 갱신 시 매달 `_43`을 0으로 리셋(월초 자동화 만들기 전까진 수동).

### 15.6 진단 입력 필드 파이프라인 (타깃·목표·경쟁사, 2026-07-30 추가)

**배경:** 타깃·목표·경쟁사를 모른 채 진단하면 마르코가 방향을 추측으로 지어냄(예: "진짜 타깃은 B2B" 같은 근거 없는 단정). 폼에 3개 질문을 추가해 입력 기반 진단으로 전환.

**폼 A 질문 (지역 다음, 채널 선택 앞):**
- `핵심고객` — "이 브랜드의 핵심 고객은 누구인가요?" (필수, 장문)
- `브랜드목표` — "3~6개월 안에 이루고 싶은 목표는?" (필수, 장문)
- `경쟁사` — "경쟁/벤치마킹 브랜드가 있나요?" (선택, 장문)

**데이터가 흐르는 5곳 (모두 완료):**
1. 폼 A Submit 코드 → 웹훅 payload에 3필드 포함
2. Make 웹훅 → Detect(29개 감지). ★주의: Detect는 "누른 뒤 오는 제출"만 잡음 → 대기 상태에서 값 넣어 제출해야 함
3. Make JSON User content → "업종" 다음에 핵심고객·브랜드목표·경쟁사 3줄 추가 (뇌에 전달 — 진단 정확도 핵심)
4. Make Airtable Create(4, 진단기록 테이블) → 필드 3개(Long text) 추가 + 매핑. ★Airtable에 필드 추가 후 Make에서 Table Refresh해야 새 필드 나타남
5. 검수 메일 Gmail(요청 정보) → 핵심 고객·브랜드 목표·경쟁사 3줄 표시

**시스템 프롬프트 규칙(반영 완료):** 입력 있으면 그 기준으로 진단(타깃은 "말한 타깃 vs 데이터 타깃" 비교, 목표는 격차 진단). **입력 없으면 추정하되 "마르코가 추정함" 명시.** 경쟁사 입력 있으면 분석, 없으면 제안(엉뚱한 경쟁사 금지).

**검증:** 로터리픽 테스트 리포트에서 시장분석이 입력 타깃(30~40대 여성) 기반으로 생성되고, 채널·제품 미입력 항목은 "⚠️ 입력되지 않아 추정함"으로 명시됨 → 추측 진단 문제 해결 확인.

---

## 16. 진행 순서 (다음 세션용 체크포인트)

```
✅ [0] 배포 URL 정리 (marcoai.kr → v10, index.html)
✅ [2-1] Zite 문지기 코드 (폼 A)
✅ 문지기 테스트 통과 (신규 저장 / 재제출 차단 / 채널없음 제출 정상)
✅ 폼 A 다듬기 (파일 첨부 제거 · "운영 채널 없음" 배타선택 추가)
✅ 랜딩 v10: 8섹션·카피·가격앵커·모바일·버튼 전부 폼 A로 통일·Pro바로신청 강조박스·다시진단 링크 제거
✅ 샘플 리포트 v2: 하단 CTA 정리(무료+Pro 통합)·공유링크 marcoai.kr
✅ 랜딩 v10 · 리포트 v2 GitHub 배포 (marcoai.kr 라이브 확인)
✅ 결제 신청 폼 개조 (ut4ec57xwv)
✅ 결제 Make 웹훅(marco-payment) + 알림 메일 완성 (한국시간 formatDate, 실발송 확인)
✅ [2-2] 진단 엔진 재설계 핵심 완료:
   - JSON 모듈 System 프롬프트 → 18장 구조로 교체(marco_diagnosis_system_prompt_v2.txt), Max tokens 4000→16000
   - 출력 형식 방향1 확정: STATUS 파이프 + ===REPORT=== 본문 + ===GUIDE=== 실측 체크리스트
   - 성공경로 Gmail: 사용자 발송 → 마르코 팀 검수 발송(To=marco.ai.kr@gmail.com, 제목 "[검수대기] 진단 리포트 - [브랜드]([담당자]"), 본문=요청정보+입력자료+1차결과물 3종세트
   - 실제 테스트 성공: 새 구조 리포트(5차원·9블록·[실측 필요] 플레이스홀더·선행지표·미션·GUIDE) 검수 메일 정상 도착. 차별화 핵심(수치 미조작+실측가이드) 작동 검증
   - 메일2(빈 알림 Gmail) 삭제
   - fallback 메일 정리: STATUS/REASON 코드 노출 제거→친절 안내 문구, 링크 폼A(f18m3zrtk8)로, "무료 차감 안 됨" 안내
✅ [2-2] 차감 구조 방향B 적용: 문지기 신규생성 _38=0 + 기존 비Pro는 _38<1이면 무료 허용. 무료 차감은 Make 성공경로(_38+1)에서만 → "성공 시 1회 차감", insufficient는 차감 안 됨(복구 불필요). (테스트 확인 필요: 신규정상→_38=1 / 부실→_38=0유지 / 재제출→허용 / 소진→차단)
🔜 [2-2 잔여] 파싱(Tools) 점검: 새 ===REPORT=== 형식이 진단기록 필드에 제대로 저장되는지
🔜 [2-2 잔여] Router fallback 조건이 STATUS=insufficient 잡는지 확인
🔜 [2-2 잔여] 첫 리포트 품질 검토(실제 클라이언트 데이터로), 모델 결정(Sonnet vs Opus 골든셋)
🔜 [2-3] 유료 입금확인 트리거 (결제기록 입금완료 → Watch)
🔜 발송(C): 검수 후 상태변경→사용자 발송(현재 수동)
🔜 이용자DB 테스트 데이터 삭제 (론칭 직전)
🔜 [3] 월초 _43 리셋 자동화 / HTTPS·카카오 디버거
🔜 [4] 해자: 모델 최적화(Sonnet+캐싱+배치, 골든셋 검증) → 뉴스레터 → 벤치마크 → 커뮤니티
```

---

## 17. 검수 프로세스 (진단 결과물 생성 → 검수 → 발송)

> **원칙:** 자동은 "1차 텍스트 생성"까지. 검수·PDF·발송은 사람(수동). 품질이 생명인 초기 구조.

### 17.1 6단계 골격

| 단계 | 주체 | 자동/수동 | 내용 |
|---|---|---|---|
| ① 요청 접수 | 사용자 | 자동 | 폼 A(진단) 또는 요청 폼(작업) 제출 |
| ② 문지기 판단 | 시스템 | 자동 | 신규/무료소진/Pro/한도 분기 (15.2) |
| ③ 1차 결과물 생성 | 마르코 뇌 | 자동 | **템플릿 구조로** 텍스트 생성 → 마르코 팀 메일 |
| ④-a 실측 채우기 | 마르코 팀 | **수동(필수)** | 채널을 직접 열어 캡처·조사 → 뇌가 표시한 `[실측 필요]` 자리를 실제 데이터로 채움 |
| ④-b 다듬기 | 마르코 팀 | **수동** | 텍스트 직접 수정(기본) / 큰 수정 시 뇌 재생성 |
| ⑤ PDF 제작 | 팀+뇌 | **수동** | 진단→build_marco_report.py / 제안서 등→요청유형별 스킬·에이전트 |
| ⑥ 최종 발송 | 마르코 팀 | **수동** | PDF를 사용자에게 이메일 발송 → 상태=발송완료 |

**Make(자동) 범위 = ①~③까지.** ④⑤⑥은 사람. 사용자에게 가는 자동 발송 없음.

**왜 완전 자동화가 안 되는가(핵심):** 채널별 상세 실측(쿠팡·스토어·홈페이지의 리뷰수·가격·베스트셀러·브랜드 혼재율 등)은 **사람이 실제 채널을 열어 캡처·조사해야만** 나온다. 폼 입력값만으로는 불가능. 즉 ④-a는 품질 다듬기가 아니라 **뇌 골격에 사람이 실측을 채우는 필수 협업**이며, 이게 곧 경쟁자가 카피 못 하는 차별점(AI 요약이 아닌 실측 기반).

### 17.2 ③ 마르코 팀 알림 메일 3종 세트 (필수)

검수하려면 한 메일에 다 있어야 함: **① 무엇을 요청받았나(요청유형) · ② 입력된 자료 전체(폼 원본) · ③ 1차 결과물(템플릿 구조 텍스트).**

### 17.3 검수 방식 (확정) — 2종 작업

- **④-a 실측 채우기 (필수, 사람만 가능):** 뇌가 1차 텍스트에 `[실측 필요: 쿠팡-리뷰수/가격/베스트셀러 …]` 형태로 표시한 자리를, 운영팀이 채널을 열어 캡처·조사해 채운다. 채널별 상세 실측표·실측 확인표가 여기서 완성됨.
- **④-b 다듬기:** 기본 = 직접 수정(오타·수치·표현). 필요 시 = 뇌 재생성(크게 다시 쓸 때만 "1차 텍스트 + 수정 지시" 재호출).

### 17.4 PDF 생성 (확정)

- **진단 리포트** → `build_marco_report.py` (body.html 작성 → 실행). 8장 스크립트 지침 참조.
- **제안서 초안·미팅 스크립트·콘텐츠 기획 등** → 요청유형별 스킬/에이전트 사용.
- **전제:** ③에서 뇌가 **처음부터 PDF 템플릿 구조에 맞게** 출력 → 검수·수정·PDF화가 쉬움.

### 17.5 상태 필드 흐름

`검수대기 → (검수중) → 발송완료`. 운영자는 진단기록에서 **상태=검수대기**만 필터 → "지금 할 일" 큐. "검수대기" = 뇌가 1차 텍스트 저장 직후, 아직 사람 손 안 탄 상태.

### 17.6 상황별 ③ 발동 조건

| 상황 | ③(뇌) 발동 | 비고 |
|---|---|---|
| A. 무료 첫 진단(신규) | 접수 즉시 자동 | 결제 없음 |
| B. Pro 진단(잔여>0) | 접수 즉시 자동 | 월요청횟수 +1 |
| C. Pro 작업 요청(제안서 등) | 접수 즉시 자동 | 요청유형별 프롬프트 분기 |
| D. 건당·무료소진 | **입금 확인 후 수동 트리거** | 문지기가 웹훅 막음(NEED_PAYMENT) |
| E. Pro 한도초과 | 발동 안 함 | "한도 소진" 안내만 |

---

## 18. 진단 리포트 섹션 구조 (Make 프롬프트 출력 규격)

> 3개 실제 사례(무료 샘플 v2·청춘양말·SPAID) 종합. **필수 골격(무료 포함) + 소수 선택 블록** 구조. 9블록·채널실측·선행지표는 무료 필수·차별화 핵심. 뇌가 골격·판단을 만들고, 채널 실측은 사람(④-a)이 채우는 협업.

### 18.1 필수 골격 (무료 리포트에도 항상 포함, 순서 고정)

1. **헤더** — 로고 / "마케터의 세 번째 뇌 · Act. Grow. Repeat." / 날짜 / 배지(Pro 진단·내부 검토용 등)
2. **인사 + 제목** — "안녕하세요, ○○ 사장님" + 클라이언트명 + 리포트 부제
3. **한 줄 요약 + 총점** — 총점(100) 또는 2점 구조(병목/자산) + 핵심 진단 한 줄
4. **핵심 태그 3종** — 업종 / 핵심 자산 / 핵심 병목
5. **실측 확인표** — "마르코가 직접 본 것" (채널·업무 실측, 추측 아닌 근거) ★④-a 실측 채움
5-1. **시장 분석** ★표준 — 시장 구조(성장/포화/레드오션), 대형 경쟁자 장악 영역, 선점할 빈 자리(니치)
5-2. **타깃 분석** ★표준 — B2B/B2C 구분, 검색 데이터 기반 실제 타깃 vs 겨냥한 타깃 일치 여부
5-3. **경쟁사 분석** ★표준 — 경쟁 그룹 2~3층 표(그룹/강점/빈틈), 구체 수치는 [실측 필요], 이길 축 진단
5-4. **키워드 분석** ★표준 — 검색량·콘텐츠 포화·연관 키워드·검색 의도(블랙키위 실측), 롱테일/니치 전략
6. **5차원 스코어링** — FC·TG·TR·MS·RT 각 20점 + 상태(양호/보통/문제) + 근거 + 병목 지목 + "숨은 경고"
7. **9블록 시스템 재진단** ★필수·차별화 핵심 — 상품/잠재고객/문제·욕구/목표/포지셔닝/콘텐츠/미디어/전환/데이터 9개, 각 상태(신규과제/양호/핵심문제/미설정 등) + 진단. 5차원이 "어디가 막혔나"라면 9블록은 "구조적으로 왜 막혔나". 리포트 깊이의 핵심.
8. **채널별 상세 실측·전략** ★필수 — 쿠팡/스토어/홈페이지 등 채널별 상품구조·물류·전략. ★④-a에서 캡처·조사로 채움.
9. **선행지표 정의** ★필수 — 결과지표(예: 자동화 80%) → 측정 가능한 선행지표(예: 주간 물류시간)로 전환한 대비표.
10. **미션** — 오늘/이번 주 실행(우선순위·소요시간·비용)
11. **마르코의 한마디** — 정서적 마무리 + 다음 달 예고
12. **푸터** — 문의처(hello@marcoai.kr·@marcoai_official) / 슬로건 / "AI+마르코 팀 직접 분석" 신뢰 문구

### 18.2 선택 블록 (병목·상황 따라, 주로 유료)

- 월별 실행 캘린더 (주차별 트랙)
- 유료 전용: 경쟁사 분석 / 제안서 초안 / 미팅 스크립트 / 업셀링 블록

### 18.3 Make 시스템 프롬프트 규칙 (③ 출력)

뇌 출력은 **두 부분**으로 나뉜다:

**A. 뇌가 완성하는 것 (자동):**
- 5차원 스코어링 + 근거 + 병목 지목
- **9블록 재진단** (폼 입력 기반 구조 진단 — 차별화 핵심)
- 선행지표 정의 (결과지표 → 선행지표)
- 미션 / 한 줄 요약 / 마르코의 한마디

**B. 뇌가 "자리만 표시"하는 것 (④-a에서 사람이 채움):**
- 채널별 상세 실측·실측 확인표 → `[실측 필요: 채널명 - 확인항목]` 플레이스홀더
- **실측 가이드 동봉:** 뇌가 "이 클라이언트는 쿠팡·인스타 운영 → 쿠팡=리뷰수·가격·베스트셀러 / 인스타=팔로워·게시물수·최근 컨셉을 확인하라"는 **채널별 실측 체크리스트를 함께 생성**해 운영팀 작업을 안내.

**공통 규칙:**
- 18.1 필수 골격 1~12를 **항상** 채운다(9블록·채널실측·선행지표 포함).
- 출력은 **build_marco_report.py의 body.html 섹션에 바로 넣을 수 있는 구조**로.
- 근거는 실측 기반("추측 아님"), 미션은 실행 가능(비용·시간 명시).
- 기존 파이프 파싱(STATUS·5차원 점수)은 유지하되, 리포트 본문을 구조화 텍스트로 확장.

### 18.4 리포트 형식 재사용 워크플로우 (형식 유지)

표준 리포트 형식을 매번 동일하게 유지하기 위한 3층 고정:
1. **템플릿(CSS)**: GitHub `marco_report_reference_pdf.html`의 head(폰트·로고·CSS 클래스)를 고정 재사용. 클래스: hd/cover/sec/kw-table-flex/score-table-flex(score-bar·score-fill)/flow-bar/insight(red·green)/channel-card/mission-card/ft. `build_marco_report.py`가 이 head를 캐시.
2. **섹션 구조**: 18.1의 순서 고정 — 헤더→커버→실측표→[시장·타깃·경쟁사·키워드 분석]→5차원→9블록→채널실측→선행지표→미션→한마디→CTA→푸터.
3. **생성 로직**: Make System 프롬프트(v2)가 위 섹션을 항상 출력. 실측 필요 항목([실측 필요]+GUIDE)은 사람이 검수 단계에서 채움.
- **PDF 제작**: body.html만 작성(로고는 head에 내장) → head+body 결합 → Playwright A4 렌더(print_background) → qpdf/pdfinfo 검증.
- **첫 실사례**: 바나로 리포트(11p) — 이 구조를 처음 끝까지 적용. 이후 리포트는 이 형식을 따른다.

---

---

## 19. Pro 챗봇 시스템 (마르코 AI 사수, 2026-08-01 구축 완료)

> Pro 이용자 전용 챗봇. "고객지원 봇"에서 "AI 사수 동반자"로 확장. 코드 인증 + 일일 한도 + 비용 최적화까지 라이브.

### 19.1 역할 (3가지)
1. **마르코 활용법** — 진단 요청·플랜·미션 피드백·재진단 안내
2. **리포트 해석·활용** — 5차원·9블록·[실측필요]·손실vs이득·미션(가설검증)을 함께 읽고 코칭
3. **마케터 성장 코칭** — 실무 고민·클라이언트 대응·판단 기준 세우기. 정답 암기가 아니라 사수처럼 렌즈를 준다.

### 19.2 아키텍처
```
챗봇 HTML(marcoai.kr/marco_chat.html)
  → POST /chat {messages, code}
  → Cloudflare Worker(marco-chat-api)
      → 코드 인증(KV) + 일일 한도 검사
      → Claude Haiku 4.5 호출 (프롬프트 캐싱 + 맥락 최근 12개)
  → {success, message, remaining}
```
- **모델**: `claude-haiku-4-5-20251001` (Sonnet 대비 비용 4배↓, 품질 검증 통과). 진단 엔진은 별개(Sonnet).
- **소스 오브 트루스**: 챗봇 시스템 프롬프트는 **Worker의 `MARCO_CHAT_PROMPT`**에 있음(HTML 아님). 파일: marco_chat_system_prompt_v2.txt.

### 19.3 접근 통제 = 개인 코드 (방식 C)
- Pro 결제 확인 → 개인 코드 발급(`MARCO-XXXX`, 헷갈리는 문자 제외).
- KV(`marco-codes`, 바인딩명 `MARCO_KV`)에 저장: 키 `code:MARCO-XXXX` → `{plan, email, expires, dailyLimit:40, issued}`.
- 링크에 코드 포함(`?code=MARCO-XXXX`) → 클릭 시 자동 인증(localStorage 저장). 코드 없으면 입력 게이트.

### 19.4 일일 한도 (남용 방어)
- 날짜별 카운트 키 `count:CODE:YYYY-MM-DD`, TTL 2일(자동 만료라 리셋 불필요). 기본 40회/일.
- 초과 시 "오늘 한도 도달, 내일 이어가요" 안내.

### 19.5 비용 최적화 (회원 늘수록 손해 방지)
- **Haiku 모델** + **프롬프트 캐싱**(system을 cache_control ephemeral → 캐시히트 90%↓) + **맥락 자르기**(최근 12메시지) 3중.
- 헤비 유저도 월 수천 원대로 상한(일일 한도까지). 일반 유저는 Pro료의 1%대.

### 19.6 Cloudflare 설정 (Worker: marco-chat-api)
| 종류 | 이름 | 용도 |
|---|---|---|
| KV 바인딩 | `MARCO_KV` → marco-codes | 코드·카운트 저장 |
| 변수(Secret) | `ANTHROPIC_API_KEY` | Claude 호출 (★이름 정확히, API_KEY 아님) |
| 변수(Secret) | `ADMIN_KEY` | 코드 발급 보호 (marco-admin-9x4x1x7x) |

- 엔드포인트: `POST /chat`(챗봇), `POST /admin/issue`(코드 발급, ADMIN_KEY 필요), `GET /`(상태), `POST /diagnose`(진단, 기존).

### 19.7 노출 지점 (메일)
- **리포트 발송 메일**: 검수 완료 리포트를 Pro에게 보낼 때 챗봇 링크(리포트 보며 질문 — 킬러 진입점).
- **Pro 환영 메일**: 결제 확인 후 개인 코드 + 챗봇 링크 + 사용법. 파일: marco_email_templates.md.

### 19.8 운영 도구·파일
- 코드 발급: **marco_code_issuer.html** (ADMIN_KEY 입력 → 코드+링크 발급). KV 대시보드 수동 입력도 가능.
- 모델 비교: marco_chat_model_compare.html (Haiku vs Sonnet 실측 비교).
- 챗봇: marco_chat.html / Worker: worker.js / 프롬프트: marco_chat_system_prompt_v2.txt.

### 19.9 남은 것 (선택)
- 리포트 해석 역할 실사용 테스트, 일일 한도 동작 확인, 기존 `API_KEY` 변수 정리(무해), 코드 만료·갱신 운영 루틴.

---

---

## 20. 마르코 뇌 성장 전략 (2026-08-01)

> 핵심 전제: **LLM 자체는 쓴다고 자동으로 똑똑해지지 않는다.** 마르코 뇌(Claude)는 매 진단마다 같은 상태로 시작. "성장"하는 것은 뇌를 둘러싼 **시스템 3층**이다. 진짜 해자 = 사람의 배움·클라이언트 히스토리·효과 검증이 버려지지 않고 쌓이는 구조.

### 20.1 성장하는 3개 층
1. **지식층** — 뇌가 참고하는 것 (전자책 증류 SKILL, 병목 처방 원칙, 벤치마크). 현재: 수동 추가.
2. **기억층** — 클라이언트 히스토리 (지난 진단·미션 결과). 현재: Airtable에 저장만, 진단 시 미주입.
3. **피드백층** — 뭐가 효과 있었나 (검수 수정, 실행 성과). 현재: 수정해도 다음 진단에 반영 안 됨 → 배움 증발.

### 20.2 3단계 로드맵 (우선순위순)

**1단계 · 피드백 루프 (🥇 비용0·즉시·임팩트 최대) — 성장 항목 [진행]**
- 검수 시 "무엇을 왜 고쳤나"를 기록 → 반복 수정 패턴 발견 → 시스템 프롬프트에 규칙화 → 같은 실수 제거.
- = 진짜 학습(사람이 고친 걸 뇌가 다음부터 스스로). 월 1회 "수정 패턴 → 프롬프트 개선" 루틴.

**2단계 · 클라이언트 기억 활성화 (🥈 Pro 핵심 가치 실현) — 성장 항목**
- 재진단 시 지난 진단 결과·미션 성과를 프롬프트에 자동 주입 → "지난달 병목 MS, 이번엔 개선됐나" 비교 진단.
- "이 클라이언트를 기억하는 세 번째 뇌" 컨셉의 실제 구현. 회를 거듭할수록 그 클라이언트에 정확해짐.

**3단계 · 지식 자동 확장 (🥉 정석·개발 큼·후순위)**
- RAG: 전자책·업계자료 벡터DB화 → 진단 시 관련 부분만 자동 검색·주입(프롬프트 안 터짐).
- 벤치마크DB: 진단마다 업종 데이터 축적 → "업종 평균 대비" 비교 자동 정확화.
- insane-search: 최신 시장·경쟁사 데이터 자동 수집 → 뇌가 항상 최신 정보로 진단.

### 20.3 핵심 원칙
"자동으로 알아서 크는 뇌"는 없다. 하지만 "사람의 배움이 안 버려지고 쌓이는 구조"는 만들 수 있다. 경쟁자가 GPT를 써도 마르코엔 수백 번의 검수·히스토리·효과검증이 쌓인 뇌가 있다 — 이것이 복제 불가능한 해자.

### 20.4 클라이언트 정의·식별 (2026-08-01 확정)
**"클라이언트 3개 슬롯" 개념 폐기.** 대행사 마케터는 클라이언트가 유동적(월 단위로 교체·추가·유지, 영업/피칭용 일회성 다수)이라 고정 슬롯이 안 맞음. 대신:
- **Pro = 월 10회 진단 + 브랜드별 자동 기억 (클라이언트 수 무제한).**
- **식별 = 이용자 이메일 + 브랜드명(정규화).** 이메일은 이용자(마케터) 것이라 단독으론 브랜드 구분 불가, 브랜드명 단독은 오타에 취약 → 둘의 조합 + 정규화.
- **브랜드명 정규화 규칙:** 앞뒤 공백 제거 / 영문 대소문자 통일 / 괄호·특수문자·"(주)"·"㈜" 정리. 흔한 오타 흡수 목적.
- **한계·다음 단계:** 정규화는 완벽한 식별이 아님. 재진단·클라이언트 관리가 본격화되면 **"클라이언트 선택 드롭다운(고유 ID)"**으로 전환해 오타 원천 차단(마르코 컨셉 "기억하는 뇌" 완성형). 로그인·마이페이지 구조 필요하므로 후순위.
- **영향:** 랜딩·가격의 "클라이언트 3곳" 문구를 "월 10회 진단"으로 정리 필요. 진단차수는 "이메일+정규화 브랜드명" 기준으로 카운트.


---

*이 문서는 확정 스냅샷이다. 변경이 생기면 해당 장을 갱신하고 최종 확정일을 바꾼다.*
*갱신 이력:*
*- 2026-07-28 v1.1 — 해자 전략(14장)·5차원 정의(부록A)·확정 카피(부록B) 추가, Pro 한도 10 반영.*
*- 2026-07-28 v1.2 — 문지기·3개의 문·필드 키·결제 흐름·운영자 절차(15장) + 진행 순서(16장) 추가.*
*- 2026-07-30 v1.3 — 문지기 테스트 통과·폼 A 다듬기(첨부 제거·채널 없음)·랜딩/리포트 배포·결제 웹훅(marco-payment)+알림메일 완성 반영. 남은 핵심: [2-2] Make 재설계(진단 A·B·C).*
*- 2026-07-30 v1.4 — 검수 프로세스(17장: 6단계·검수방식·PDF·상황별 발동) + 진단 리포트 섹션 구조(18장: 필수 골격 9 + 선택 블록, 3개 실사례 종합) 추가. 진단 흐름부터 Make 재설계 착수.*
*- 2026-07-30 v1.5 — 9블록·채널실측·선행지표를 무료 필수 골격으로 승격(차별화 핵심). 검수를 ④-a 실측 채우기(사람 필수)+④-b 다듬기로 분리. 뇌는 골격·판단·실측가이드 생성, 채널 실측은 사람이 채우는 협업 구조로 확정.*
*- 2026-07-30 v1.6 — 진단 엔진 재설계 핵심 완료: System 프롬프트 v2 교체·Max 16000·검수 발송 전환·실제 테스트 성공([실측 필요]+GUIDE 작동)·메일2 삭제·fallback 메일 정리. 잔여: insufficient 차감 복구, 파싱 점검, 품질 검토, 모델 결정.*
*- 2026-07-30 v1.7 — 차감 구조 방향B로 확정·적용: 문지기 신규 _38=0 생성 + 기존 비Pro는 _38<1이면 무료 허용. 무료 차감은 Make 성공경로에서만(성공 시 1회). insufficient는 차감 안 됨→복구 로직 불필요. 문지기 코드 수정 완료, 테스트 대기.*
*- 2026-07-30 v1.8 — 리포트 표준 섹션에 시장·타깃·경쟁사·키워드 분석 4개 추가(18.1). System 프롬프트 v2에 반영. 형식 재사용 워크플로우(18.4) 문서화. 바나로 첫 실제 리포트(11p PDF) 생성 완료.*
*- 2026-07-30 v1.9 — 진단 입력 필드 파이프라인(15.6): 핵심고객·브랜드목표·경쟁사 3필드를 폼→웹훅→JSON→Airtable(진단기록)→검수메일 5곳에 연결. 시스템 프롬프트에 "입력 우선, 없으면 추정 명시" 규칙 반영. 로터리픽 테스트로 추측 진단 문제 해결 검증. 리포트 섹션에 시장·타깃·경쟁사·키워드 분석 추가.*
*- 2026-08-01 v2.0 — Pro 챗봇 시스템(19장) 구축 완료: 고객지원봇→AI 사수(3역할)로 확장. Haiku 4.5 + 코드 인증(KV, 방식C) + 일일 한도 40 + 프롬프트 캐싱·맥락자르기(비용 최적화). Worker(marco-chat-api) 재설계·배포, HTML 코드게이트, 코드 발급 도구, 메일 템플릿 2종(리포트·환영). Cloudflare 변수 MARCO_KV·ANTHROPIC_API_KEY·ADMIN_KEY 설정. 실제 대화 테스트 성공(활용·성장코칭 검증).*
*- 2026-08-01 v2.1 — 뇌 성장 전략(20장): LLM은 자동으로 안 큼, 시스템 3층(지식·기억·피드백)이 성장. 로드맵 1)피드백 루프 2)클라이언트 기억 활성화 3)RAG·벤치마크. 핵심 해자=사람의 배움이 쌓이는 구조.*
*- 2026-08-01 v2.2 — 클라이언트 정의 확정(20.4): "3개 슬롯" 폐기 → "월 10회 진단+브랜드별 자동기억(무제한)". 식별=이메일+브랜드명(정규화: 공백·대소문자·괄호·(주) 정리). 완벽식별(드롭다운)은 후순위. 랜딩 "클라이언트 3곳"→"월 10회" 정리 필요.*
